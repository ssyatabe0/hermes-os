# AGENTS.md — Hermes OS（東京8区 地域サービス・データ事業）

このファイルは、このリポジトリで作業する人間・AIエージェント（Claude Code含む）が
**過去の会話ログを読み返さなくても作業を再開できる**ようにするための起点ドキュメントです。

作業を始める前に、必ず次の順で読んでください。

1. `AGENTS.md`（このファイル） — プロジェクトの目的・構造・ルール
2. `STATUS.md` — 現在地、完了/未完了、判断待ち事項
3. `TODO.md` — 優先順位付きの次アクション
4. `DECISIONS.md` — これまでの意思決定と理由

**すでに調査・実装済みの内容を理由なく再調査・再実装しないこと。**
（`STATUS.md` の「完了済み」セクションと `db/hermes.db` の中身を必ず先に確認する）

---

## 1. プロジェクトの本質

このプロジェクトは「リフォームサイト」でも「アフィリエイトサイト」でもない。

最終資産は **データ** である:

> 地域 × 困りごと（サービス） × 顧客属性 × 季節 × 収益性

を継続的に実測し、

> どこで・誰が・いつ・何に困っていて・どのサービスなら利益を出して獲得できるか

を自社独自データとして蓄積すること。サイトやブログサービスは手段であり、
CMSやブログサービスを乗り換えても **データは一切失わない設計** を維持すること。

事業モデル:

```
低コスト集客(SEO/SNS) → 無料ブログ → 需要計測 → アフィリエイト送客
  → 検索/クリック/問い合わせ/成果データ蓄積 → 地域×サービス収益性判定
  → 勝てる市場だけ自社受付化 → 地域パートラーへ発注 → 粗利獲得
  → リピート → 年間契約・月額住宅管理
```

最終形は「格安便利屋」ではなく、**富裕層向け住宅コンシェルジュ／ホームマネジメント会社**。
理想状態＝「住宅について困ったら、とりあえずここへ連絡すればいい」。

## 2. 初期商圏（東京8区）

最優先: 港区・品川区
重点: 渋谷区・大田区・千代田区・世田谷区・新宿区・目黒区

DB設計は全国展開を前提に汎用化するが、**記事生成・SEO・需要分析・自社化候補選定は
この8区に限定する**。全国向け・積雪地向け（将来候補として除雪サービスのみDBに保持）の
コンテンツを今の段階で大量生成しない。

## 3. ディレクトリ構成

```
/AGENTS.md          このファイル
/STATUS.md           現在地・進捗・判断待ち事項
/TODO.md             優先順位付きタスクリスト
/DECISIONS.md        意思決定ログ（決定/理由/代替案/変更履歴）
/db/
  schema.sql          全テーブル定義（SQLite）
  seed.sql            初期投入データ（実測 or 仮説、必ずconfidence列で区別）
  init_db.py          schema.sql + seed.sql から db/hermes.db を構築するスクリプト
  README.md           DB運用ルール・テーブル一覧・クエリ例
  hermes.db           実データベース本体（init_db.py実行後に生成、gitに含める）
/research/
  01_region_analysis.md          8区の地域分析（区〜町丁目レベル）
  02_service_catalog_and_licensing.md  対象サービス一覧・許認可調査
  03_affiliate_asp_landscape.md  ASP・アフィリエイト案件調査
  04_free_blog_cms_comparison.md 無料ブログ/CMS比較・選定理由
  05_pricing_cost_margin.md      受注価格・原価・粗利の仮説と調査結果
  06_affluent_demand.md          富裕層特有の需要調査
/rankings/
  01_top10_region_service.md     東京8区で最も勝ちやすい地域×サービスTOP10
  02_self_operate_candidates.md  自社化候補TOP5
  03_affiliate_keep.md           アフィリエイト継続候補TOP5
  04_affluent_ltv.md             富裕層LTV最大化候補TOP5
  05_annual_contract.md          年間契約・月額契約候補TOP5
  06_btob_candidates.md          BtoB継続受注候補TOP5
```

将来追加予定（今回は未着手、`TODO.md` 参照）:
`/content/`（実際の記事）, `/dashboard/`（計測ダッシュボード）, `/tracking/`（GSC/GA/ASP連携）

## 4. データ設計の原則（厳守）

1. **実測値と推定値を必ず区別する。** すべての数値データに
   `data_type`（`actual` / `estimate`）, `confidence`（`high`/`medium`/`low`）,
   `source_url`, `collected_at` を持たせる。出典のない数値は `estimate` かつ
   出典欄に「仮説（根拠：一般知識/類推）」と明記する。
2. **過去データを上書きしない。** 計測系テーブル（keyword_db, article_db,
   region_service_score, seasonal_demand 等）は **追記専用（INSERT-only）**。
   同一キーの最新値が必要な場合は `v_*_latest` ビューを使う。UPDATEするのは
   `service_catalog` や `partner_db` のような台帳・マスタ系のみ。
3. **取得不能なデータは「取得不能」と明記する。** 無いものを埋めない。
4. **許認可が必要な作業を自社でできると判断しない。** `license_requirements`
   テーブルに調査結果を記録し、有資格の協力会社への委託を前提に設計する。

## 5. 禁止事項（サマリ、詳細は元指示参照）

- 需要調査前の大量記事・大量地域ページ生成
- 架空の口コミ・施工実績・専門家コメント・料金実績
- 根拠のない市場規模の断定
- 推定値を実測値として扱う／取得不能データを取得済みとして扱う
- 規約違反アフィリエイトの実施
- 必要資格を無視した自社施工の実施
- 過去データの削除・上書き
- 完了済み調査の無意味な再実行
- 不要な有料SaaS/APIの導入（導入する場合は月額費用・必要性・無料代替案・期待効果を記録）
- 「安さ」だけを競争軸にすること

## 6. 作業の再開方法（次回セッション向け）

1. `STATUS.md` の「次にやること」を読む
2. `TODO.md` の最上位未完了タスクに着手
3. 作業完了ごとに `STATUS.md` / `TODO.md` / 必要なら `DECISIONS.md` を更新
4. DBへ変更を加えたら `db/init_db.py` を再実行し `db/hermes.db` をコミット
5. 判断が必要な事項が出たら、作業を止めずに `STATUS.md` に
   「判断事項／推奨案／理由／代替案」を記載し、他の作業を継続する
