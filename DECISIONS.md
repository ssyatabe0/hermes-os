# DECISIONS.md — 意思決定ログ

新しい決定を追加する際は末尾に追記し、既存の決定を変更する場合は元の項目に
「変更履歴」として追記する（削除しない）。

---

## D-001: 初期商圏を東京都8区に限定し、DBは全国展開前提で設計する

- 決定: 記事生成・SEO・需要分析・自社化候補選定は港区・品川区（最優先）＋渋谷区・
  大田区・千代田区・世田谷区・新宿区・目黒区（重点）の8区に限定。ただしDBスキーマの
  `prefecture`列等は全国対応できる形にする
- 理由: ユーザー指示どおり。大量の全国向けコンテンツを先に作ると、需要の薄い
  地域にコストをかけるリスクがある
- 代替案: 最初から全国のDB/コンテンツを作る → 却下（指示1・27・52に反する）
- 日付: 2026-08-15

## D-002: DBはSQLite単一ファイル（`db/hermes.db`）を採用

- 決定: PostgreSQL等のサーバー型DBではなく、SQLiteを採用
- 理由: 無料・低コスト・単一ファイルでバックアップが容易・スキーマ設計自体は
  将来PostgreSQL等へ移行しやすい標準的なSQLで書いている（指示40・42準拠）
- 代替案: Google Sheets/CSVのみ → クエリ・整合性制約（CHECK制約等）が弱く、
  データ量が増えた際の分析に不向きなため却下。Supabase等の有料/準有料SaaS →
  現時点で必要性がなく、指示42「不要な有料SaaS導入をしない」に反するため却下
- 日付: 2026-08-15

## D-003: 計測系テーブルは追記専用（INSERT-only）、最新値はビューで取得

- 決定: keyword_db, article_db, region_service_score, seasonal_demand,
  customer_orders 等は UPDATE せず、常に新しい行を追加する。現在値が必要な画面・
  分析には `v_keyword_latest` 等のビューを使う
- 理由: 指示16「過去データを消さない」を機械的に担保するため。UPDATE文を使う
  運用にすると、将来誰か（人間もAIも）が誤って上書きするリスクが残る
- 代替案: 履歴テーブルを別に持ち、メインテーブルはUPDATEする方式 → 二重管理に
  なり実装・運用が複雑化するため却下
- 日付: 2026-08-15

## D-004: 無料ブログは「はてなブログ」を初期採用

- 決定: note・アメーバブログではなく、はてなブログ（無料枠→反応が良ければPro）を
  最初のテストブログとして採用
- 理由: 外部ASPアフィリエイトが規約上可能（noteは基本NG、アメブロは独自Ameba Pick
  中心）。はてなブックマーク経由の初動流入が期待できる。Pro版で独自ドメイン・
  解析タグ設置の自由度が高く、将来の資産化（WordPress等への移行）がしやすい
- 代替案: 最初から独自ドメイン+WordPress → 初期費用は小さいが「無料で需要を実測する」
  という指示の趣旨を優先し保留。note → 外部ASP不可のため却下。アメブロ →
  Ameba Pick中心で任意のASP案件を試しにくいため却下（SNS的補助チャネルとしては
  将来検討の余地を残す）
- 判断保留事項: `STATUS.md`の判断待ち事項2に記載。ユーザーが変更を希望すれば
  いつでも切替可能
- 日付: 2026-08-15

## D-005: 最初にSEOテストする5市場を選定

- 決定: (1)港区×エアコンクリーニング (2)品川区×エアコンクリーニング
  (3)品川区×給湯器交換 (4)世田谷区×庭木剪定・伐採 (5)目黒区×ハウスクリーニング
- 理由: 検索意図が業者探し/緊急/料金寄りで問い合わせ転換しやすい、外注しやすい
  （エアコン・清掃・剪定は許認可障壁が低い）、季節が分散し年間を通じて記事を
  出せる、最優先2区＋重点区を組み合わせてポートフォリオ的にテストできる
- 代替案: 港区×定期住宅点検・不在宅管理（富裕層LTV最大化候補）を最初の5候補に
  含める案 → 検索需要自体がまだ顕在化しておらず、SEO初期テストの母数確保が
  難しいと判断し、`rankings/04_affluent_ltv.md`の中期候補として保留
- 判断保留事項: `STATUS.md`の判断待ち事項1に記載
- 日付: 2026-08-15

## D-006: ダッシュボード・自動発注・パートナー実データ投入は今回のスコープ外

- 決定: 指示31〜35のダッシュボード機能、指示25・26の発注マッチングロジック、
  パートナーDBへの実データ投入は本セッションでは実装しない。DBスキーマ上は
  対応できる形で設計済み
- 理由: 実測データ（GSC/GA4/ASP成果/問い合わせ）がまだ存在しない段階で
  ダッシュボードを作っても空の画面にしかならず、指示44「最初から100サービス・
  1000記事を作らない」と同じ思想で「先に需要とデータを作る」ことを優先した
- 代替案: 先にダッシュボードの箱だけ作る → 保守対象が増えるだけで価値を生まないため
  今回は見送り、`TODO.md` P2に次のマイルストーンとして記載
- 日付: 2026-08-15

## D-008: ライブドアブログを追加（はてなブログと並行運用）

- 決定: ユーザー指定のアカウント（ID: ssyatabe2）でライブドアブログを2つ目のテスト
  ブログとして追加する。はてなブログ（D-004）は取り下げず、並行運用しプラットフォーム
  ごとの反応差も実測データとして蓄積する
- 理由: ユーザーからの明示指示。調査の結果、ライブドアブログもアフィリエイト掲載は
  禁止されておらず（Adsense/ASP/Amazon/楽天いずれも可）、`research/04_free_blog_cms_comparison.md`
  の採用基準（外部ASP可否）を満たす。加えてAtomPub APIによる自動投稿に公式対応しており、
  記事DBとの連携がはてなブログより実装しやすい可能性がある
- **重要な訂正**: ユーザーから共有されたログインパスワードは、AtomPub API（自動投稿）の
  認証には使えない。ライブドアブログの仕様上、API認証はログインパスワードとは別の
  「Atom Pub用パスワード（APIキー）」をブログ設定画面から発行して使う設計になっている。
  アカウント本体のログインパスワードを外部の自動化スクリプトに組み込むことはセキュリティ上
  避けるべきであり、本プロジェクトではAPIキー方式のみを自動投稿に使う
- 代替案: ログインパスワードでブラウザ自動操作（ヘッドレスブラウザでのログイン）を試みる →
  却下。(1)このセッションの実行環境はユーザーのローカル端末にもブラウザにもアクセスできない
  独立したコンテナであり技術的に不可能、(2)可能だとしても2段階認証・不正ログイン検知の
  対象になり得ズアカウントロックのリスクがある、(3)公式APIが用意されているのにログイン
  情報を使う理由がない
- 日付: 2026-08-15

## D-009: 「来年までに地域の総合リフォーム店を運営したい」というユーザーの事業目標を反映

- 決定: 12ヶ月以内（2027年央目処）に「地域の総合リフォーム店」として自社受付・施工発注が
  回っている状態を目指すマイルストーンを設定する。これに伴い、次の2点を計画に反映する。
  1. リフォーム系（戸建て/マンションリフォーム、キッチン、浴室等）の**有資格協力会社の
     ソーシング（発掘・関係構築）を、SEOテストと並行して今から始める**。指示2「まず需要、
     その後供給」の原則は維持しつつ、"需要確認後に一からパートナー探しを始める"のでは
     12ヶ月という期限に間に合わない可能性が高いため、需要検証と並走させる
  2. 現在の最初の5市場（`rankings/01_top10_region_service.md`）は低〜中単価×高頻度の
     サービス（エアコン・清掃・剪定・給湯器）を中心にしているが、これは「安く早く需要を
     検証する」ための入口であり、最終形は総合リフォーム店であることを`STATUS.md`に
     明記し、今後のTODOに「大型リフォームの協力会社候補リストアップ」を追加する
- 理由: ユーザー指示。単なるSEOブログ運営ではなく、実店舗的な事業運営を来年には
  始めたいという明確なタイムライン制約が示されたため
- 代替案: 従来どおり実測データが十分貯まってから供給側を構築する完全逐次モデル →
  12ヶ月という制約に対してリスクが高いため、需要検証とパートナー探索の並走モデルに変更
- 日付: 2026-08-15

## D-010: ブラウザ自動化基盤(`automation/`)を追加。Playwright + 専用プロファイル方式を採用

- 決定: 汎用ブラウザ自動化モジュール(`automation/browser/engine.py`)をPlaywright(Python)で
  実装。ASP・ライブドアブログ等への対応は「専用Chromeプロファイル＋人間が最初の1回だけ
  手動ログイン」方式（ユーザー提案の第1候補）を採用。CDPで既存Chromeへ接続する方式や
  既存プロファイルのコピーを使う方式（第2・第3候補）は、本番Chromeプロファイルを壊す
  リスクがあるため見送り
- **重要な環境上の発見**: このClaude Codeクラウドセッションは、ユーザーのMac上ではなく
  独立したコンテナで動いている。実際に検証した結果、このコンテナのネットワークポリシーは
  `livedoor.com`・`a8.net`・`google.com`等、一般の外部サイトへのHTTPS接続を
  プロキシ側で403(policy denial)として拒否することを確認した
  （`curl -x $HTTPS_PROXY --cacert /root/.ccr/ca-bundle.crt https://example.com` で再現、
  `/root/.ccr/README.md`记載の「組織ポリシー拒否はリトライしない」方針に従いこれ以上の
  回避は試みていない）。npm/PyPI/GitHub/Anthropic等の許可リストに載ったホストのみ到達可能。
  したがって、Playwright自体は正常に動作する（ローカルの静的テストページに対する起動・
  遷移・クリック・入力・検索・テーブル抽出・リンク抽出・スクリーンショットまで実機検証済み、
  `automation/scripts/smoke_test_local.py`）が、**livedoor/A8.net/バリューコマース等の
  実サイトへは、このクラウドセッションからは技術的に到達できない**
- 結論: 実サイトへの自動アクセスが必要な作業は、ユーザーのローカルMacでClaude Code CLIを
  起動するか、人間が直接 `automation/` のスクリプトを実行する運用とする
  （`automation/README.md`に手順を記載）
- 理由: 「できません」で終了せず技術的に検証すること、というユーザー指示に従い、
  Playwright導入・ローカル動作確認・ネットワーク到達性の実地検証まで実施した上での結論
- 日付: 2026-08-15

## D-011: ASP自動操作は「規約確認済みのASPのみ、read-onlyに限定」で実装

- 決定: `automation/asp/base.py` に `TOS_REGISTRY` を設け、ASPごとに
  `automation_allowed`("unknown"/"prohibited"/"allowed_readonly") と
  `application_allowed`(既定False固定)を管理する。現時点でA8.net・バリューコマース・
  もしもアフィリエイト・アクセストレード・afbはすべて`"unknown"`とし、
  **どのASPに対しても自動情報取得・自動提携申請を実行しない**設定にした
- 理由: WebSearchでA8.net・バリューコマースの禁止事項ページを確認したが、自動アクセス/
  スクレイピング/botに関する明示条項を特定できなかった（`unknown`のまま）。ユーザー指示
  「規約上不明・禁止の場合は自動実行しない」を機械的に担保するため、コード側で
  `require_readonly_allowed()`が例外を送出するガードを実装し、実際に動作確認済み
- 代替案: 「明示的に禁止と書かれていなければ許可されているとみなす」という解釈で
  自動化を進める → 却下。規約不明を自動化OKの理由にはしない
- 提携申請(`application_allowed`)は将来規約が確認できたASPでも常にFalse固定とし、
  常に人間が最終確認して手動申請する（契約行為を自動確定させない、というユーザー指示に
  対応）。書き込み系操作全般は`BrowserSession(allow_write=False)`が既定で、
  `submit_write()`を呼んでも例外になることをテストで確認済み
- 日付: 2026-08-15

## D-007: 許認可未調査のサービスは「自社化不可」をデフォルトとする

- 決定: `service_catalog.requires_license` が未調査のサービスも、自社受付の
  可否を判定する際は「不明＝許可されるまで自社施工しない」という安全側の
  デフォルトを取る
- 理由: 指示26・27・52「必要資格を無視した自社施工をしない」「できそうだから
  自社でやると判断しない」を機械的に担保するため
- 代替案: 調査していないサービスは一旦「許可不要」とみなして自社化を進める →
  法令違反リスクがあるため却下
- 日付: 2026-08-15
